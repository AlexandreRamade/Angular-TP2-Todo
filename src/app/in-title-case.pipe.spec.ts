import { InTitleCasePipe } from './in-title-case.pipe';

describe('InTitleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new InTitleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
